apiVersion: gridscale.gridscale.platformrelay.io/v1alpha1
kind: Snapshot
metadata:
  name: observe-snapshot
  annotations:
    # Set external-name to the UUID of the existing gridscale resource to observe.
    crossplane.io/external-name: "<replace-with-uuid>"
spec:
  managementPolicies:
    - Observe
  providerConfigRef:
    name: default
  forProvider: {}
