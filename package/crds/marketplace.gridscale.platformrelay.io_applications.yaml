---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: applications.marketplace.gridscale.platformrelay.io
spec:
  group: marketplace.gridscale.platformrelay.io
  names:
    categories:
    - crossplane
    - managed
    - gridscale
    kind: Application
    listKind: ApplicationList
    plural: applications
    singular: application
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Application is the Schema for the Applications API. Manages marketplace
          applications in Gridscale.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ApplicationSpec defines the desired state of Application
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  category:
                    description: |-
                      Category of marketplace application. Accepted values: "CMS", "project management", "Adminpanel", "Collaboration", "Cloud Storage", "Archiving".
                      Category of marketplace application. Accepted values: "CMS", "project management", "Adminpanel", "Collaboration", "Cloud Storage", "Archiving"
                    type: string
                  metaAdvices:
                    description: See Argument Reference above.
                    type: string
                  metaAuthor:
                    description: Author.
                    type: string
                  metaComponents:
                    description: 'Components (e.g: MySql, Apache, etc.).'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  metaFeatures:
                    description: List of functions.
                    type: string
                  metaHints:
                    description: Hints.
                    type: string
                  metaIcon:
                    description: base64 encoded image of the icon.
                    type: string
                  metaLicense:
                    description: License number.
                    type: string
                  metaOs:
                    description: Operating system.
                    type: string
                  metaOverview:
                    description: Describes the main function of the application.
                    type: string
                  metaTermsOfUse:
                    description: Terms of use.
                    type: string
                  name:
                    description: |-
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters
                    type: string
                  objectStoragePath:
                    description: |-
                      Path to the images for the application, must be in .gz format and started with s3//.
                      Path to the images for the application, must be in .gz format and started with s3//
                    type: string
                  publish:
                    description: Whether you want to publish your application or not
                    type: boolean
                  setupCores:
                    description: |-
                      Number of server's cores.
                      Number of server's cores
                    type: number
                  setupMemory:
                    description: |-
                      The capacity of server's memory in GB.
                      The capacity of server's memory in GB
                    type: number
                  setupStorageCapacity:
                    description: |-
                      The capacity of server's storage in GB.
                      The capacity of server's storage in GB
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  category:
                    description: |-
                      Category of marketplace application. Accepted values: "CMS", "project management", "Adminpanel", "Collaboration", "Cloud Storage", "Archiving".
                      Category of marketplace application. Accepted values: "CMS", "project management", "Adminpanel", "Collaboration", "Cloud Storage", "Archiving"
                    type: string
                  metaAdvices:
                    description: See Argument Reference above.
                    type: string
                  metaAuthor:
                    description: Author.
                    type: string
                  metaComponents:
                    description: 'Components (e.g: MySql, Apache, etc.).'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  metaFeatures:
                    description: List of functions.
                    type: string
                  metaHints:
                    description: Hints.
                    type: string
                  metaIcon:
                    description: base64 encoded image of the icon.
                    type: string
                  metaLicense:
                    description: License number.
                    type: string
                  metaOs:
                    description: Operating system.
                    type: string
                  metaOverview:
                    description: Describes the main function of the application.
                    type: string
                  metaTermsOfUse:
                    description: Terms of use.
                    type: string
                  name:
                    description: |-
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters
                    type: string
                  objectStoragePath:
                    description: |-
                      Path to the images for the application, must be in .gz format and started with s3//.
                      Path to the images for the application, must be in .gz format and started with s3//
                    type: string
                  publish:
                    description: Whether you want to publish your application or not
                    type: boolean
                  setupCores:
                    description: |-
                      Number of server's cores.
                      Number of server's cores
                    type: number
                  setupMemory:
                    description: |-
                      The capacity of server's memory in GB.
                      The capacity of server's memory in GB
                    type: number
                  setupStorageCapacity:
                    description: |-
                      The capacity of server's storage in GB.
                      The capacity of server's storage in GB
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.category is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.category)
                || (has(self.initProvider) && has(self.initProvider.category))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.objectStoragePath is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.objectStoragePath)
                || (has(self.initProvider) && has(self.initProvider.objectStoragePath))'
            - message: spec.forProvider.setupCores is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.setupCores)
                || (has(self.initProvider) && has(self.initProvider.setupCores))'
            - message: spec.forProvider.setupMemory is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.setupMemory)
                || (has(self.initProvider) && has(self.initProvider.setupMemory))'
            - message: spec.forProvider.setupStorageCapacity is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.setupStorageCapacity)
                || (has(self.initProvider) && has(self.initProvider.setupStorageCapacity))'
          status:
            description: ApplicationStatus defines the observed state of Application.
            properties:
              atProvider:
                properties:
                  category:
                    description: |-
                      Category of marketplace application. Accepted values: "CMS", "project management", "Adminpanel", "Collaboration", "Cloud Storage", "Archiving".
                      Category of marketplace application. Accepted values: "CMS", "project management", "Adminpanel", "Collaboration", "Cloud Storage", "Archiving"
                    type: string
                  changeTime:
                    description: |-
                      The date and time of the last marketplace application change.
                      Defines the date and time of the last object change
                    type: string
                  createTime:
                    description: |-
                      The date and time the marketplace application was initially created.
                      Defines the date and time the object was initially created
                    type: string
                  id:
                    description: The UUID of the marketplace application.
                    type: string
                  isApplicationOwner:
                    description: |-
                      Whether the you are the owner of application or not.
                      Whether the you are the owner of application or not
                    type: boolean
                  isPublishGlobal:
                    description: |-
                      Whether a template is published to other partner or not.
                      Whether a template is published to other partner or not
                    type: boolean
                  isPublishGlobalRequested:
                    description: |-
                      Whether a partner wants their tenant template published to other partners.
                      Whether a partner wants their tenant template published to other partners
                    type: boolean
                  isPublishRequested:
                    description: |-
                      Whether the tenants want their template to be published or not.
                      Whether the tenants want their template to be published or not
                    type: boolean
                  isPublished:
                    description: |-
                      Whether the template is published by the partner to their tenant".
                      Whether the template is published by the partner to their tenant
                    type: boolean
                  metaAdvices:
                    description: See Argument Reference above.
                    type: string
                  metaAuthor:
                    description: Author.
                    type: string
                  metaComponents:
                    description: 'Components (e.g: MySql, Apache, etc.).'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  metaFeatures:
                    description: List of functions.
                    type: string
                  metaHints:
                    description: Hints.
                    type: string
                  metaIcon:
                    description: base64 encoded image of the icon.
                    type: string
                  metaLicense:
                    description: License number.
                    type: string
                  metaOs:
                    description: Operating system.
                    type: string
                  metaOverview:
                    description: Describes the main function of the application.
                    type: string
                  metaTermsOfUse:
                    description: Terms of use.
                    type: string
                  name:
                    description: |-
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters
                    type: string
                  objectStoragePath:
                    description: |-
                      Path to the images for the application, must be in .gz format and started with s3//.
                      Path to the images for the application, must be in .gz format and started with s3//
                    type: string
                  publish:
                    description: Whether you want to publish your application or not
                    type: boolean
                  publishGlobalRequestedDate:
                    description: |-
                      The date when a partner requested their tenants template to be published.
                      The date when a partner requested their tenants template to be published
                    type: string
                  publishRequestedDate:
                    description: |-
                      The date when the tenant requested their template to be published.
                      The date when the tenant requested their template to be published
                    type: string
                  publishedDate:
                    description: |-
                      The date when the template is published into other tenant in the same partner.
                      The date when the template is published into other tenant in the same partner
                    type: string
                  publishedGlobalDate:
                    description: |-
                      The date when a template is published to other partner.
                      The date when a template is published to other partner
                    type: string
                  setupCores:
                    description: |-
                      Number of server's cores.
                      Number of server's cores
                    type: number
                  setupMemory:
                    description: |-
                      The capacity of server's memory in GB.
                      The capacity of server's memory in GB
                    type: number
                  setupStorageCapacity:
                    description: |-
                      The capacity of server's storage in GB.
                      The capacity of server's storage in GB
                    type: number
                  status:
                    description: |-
                      The status of the marketplace application.
                      status indicates the status of the object
                    type: string
                  type:
                    description: |-
                      The type of template.
                      The type of template
                    type: string
                  uniqueHash:
                    description: |-
                      Unique hash to allow user to import the self-created marketplace application.
                      Unique hash to allow user to import the self-created marketplace application
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
