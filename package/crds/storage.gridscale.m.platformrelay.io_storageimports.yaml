---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: storageimports.storage.gridscale.m.platformrelay.io
spec:
  group: storage.gridscale.m.platformrelay.io
  names:
    categories:
    - crossplane
    - managed
    - gridscale
    kind: StorageImport
    listKind: StorageImportList
    plural: storageimports
    singular: storageimport
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StorageImport is the Schema for the StorageImports API. Make
          a clone of an existing storage instance.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StorageImportSpec defines the desired state of StorageImport
            properties:
              forProvider:
                properties:
                  capacity:
                    description: |-
                      The default value is inherited from the source storage instance. A desired capacity is possible. Required (integer - minimum: 1 - maximum: 4096).
                      The capacity of a storage in GB.
                    type: number
                  labels:
                    description: |-
                      List of labels in the format [ "label1", "label2" ].
                      List of labels.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: |-
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                    type: string
                  storageBackupId:
                    description: |-
                      ID of the storage backup that will be used to create a new storage from.
                      ID of the storage backup that will be used to create a new storage from.
                    type: string
                  storageType:
                    description: |-
                      The default value is inherited from the source storage instance. A desired storage type is possible. (one of storage, storage_high, storage_insane).
                      (one of storage, storage_high, storage_insane)
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  capacity:
                    description: |-
                      The default value is inherited from the source storage instance. A desired capacity is possible. Required (integer - minimum: 1 - maximum: 4096).
                      The capacity of a storage in GB.
                    type: number
                  labels:
                    description: |-
                      List of labels in the format [ "label1", "label2" ].
                      List of labels.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: |-
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                    type: string
                  storageBackupId:
                    description: |-
                      ID of the storage backup that will be used to create a new storage from.
                      ID of the storage backup that will be used to create a new storage from.
                    type: string
                  storageType:
                    description: |-
                      The default value is inherited from the source storage instance. A desired storage type is possible. (one of storage, storage_high, storage_insane).
                      (one of storage, storage_high, storage_insane)
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.storageBackupId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.storageBackupId)
                || (has(self.initProvider) && has(self.initProvider.storageBackupId))'
          status:
            description: StorageImportStatus defines the observed state of StorageImport.
            properties:
              atProvider:
                properties:
                  capacity:
                    description: |-
                      The default value is inherited from the source storage instance. A desired capacity is possible. Required (integer - minimum: 1 - maximum: 4096).
                      The capacity of a storage in GB.
                    type: number
                  changeTime:
                    description: |-
                      Defines the date and time of the last object change.
                      Defines the date and time of the last object change.
                    type: string
                  createTime:
                    description: |-
                      The time the object was created.
                      Defines the date and time the object was initially created.
                    type: string
                  currentPrice:
                    description: The price for the current period since the last bill.
                    type: number
                  id:
                    type: string
                  labels:
                    description: |-
                      List of labels in the format [ "label1", "label2" ].
                      List of labels.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  lastUsedTemplate:
                    description: Indicates the UUID of the last used template on this
                      storage (inherited from snapshots).
                    type: string
                  licenseProductNo:
                    description: |-
                      If a template has been used that requires a license key (e.g. Windows Servers) this shows the product_no of the license (see the /prices endpoint for more details).
                      If a template has been used that requires a license key (e.g. Windows Servers) this shows the product_no of the license (see the /prices endpoint for more details).
                    type: number
                  locationCountry:
                    description: |-
                      Two digit country code (ISO 3166-2) of the location where this object is placed.
                      Two digit country code (ISO 3166-2) of the location where this object is placed.
                    type: string
                  locationIata:
                    description: |-
                      Uses IATA airport code, which works as a location identifier.
                      Uses IATA airport code, which works as a location identifier.
                    type: string
                  locationName:
                    description: |-
                      The location name.
                      The human-readable name of the location. It supports the full UTF-8 character set, with a maximum of 64 characters.
                    type: string
                  locationUuid:
                    description: |-
                      The location this resource is placed. The location of a resource is determined by it's project.
                      Identifies the data center this object belongs to.
                    type: string
                  name:
                    description: |-
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                    type: string
                  parentUuid:
                    type: string
                  status:
                    description: |-
                      status indicates the status of the object.
                      status indicates the status of the object.
                    type: string
                  storageBackupId:
                    description: |-
                      ID of the storage backup that will be used to create a new storage from.
                      ID of the storage backup that will be used to create a new storage from.
                    type: string
                  storageType:
                    description: |-
                      The default value is inherited from the source storage instance. A desired storage type is possible. (one of storage, storage_high, storage_insane).
                      (one of storage, storage_high, storage_insane)
                    type: string
                  usageInMinutes:
                    description: The amount of minutes the IP address has been in
                      use.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
