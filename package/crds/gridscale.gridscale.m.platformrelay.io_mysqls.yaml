---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: mysqls.gridscale.gridscale.m.platformrelay.io
spec:
  group: gridscale.gridscale.m.platformrelay.io
  names:
    categories:
    - crossplane
    - managed
    - gridscale
    kind: MySQL
    listKind: MySQLList
    plural: mysqls
    singular: mysql
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MySQL is the Schema for the MySQLs API. Manage a MySQL service
          in gridscale.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MySQLSpec defines the desired state of MySQL
            properties:
              forProvider:
                properties:
                  labels:
                    description: |-
                      List of labels in the format [ "label1", "label2" ].
                      List of labels.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  maxCoreCount:
                    description: |-
                      Maximum CPU core count. The mysql instance's CPU core count will be autoscaled based on the workload. The number of cores stays between 1 and max_core_count.
                      Maximum CPU core count. The MySQL instance's CPU core count will be autoscaled based on the workload. The number of cores stays between 1 and `max_core_count`.
                    type: number
                  mysqlBinlogFormat:
                    description: |-
                      mysql parameter: Binary Logging Format. Default: "ROW".
                      Binary Logging Format.
                    type: string
                  mysqlDefaultTimeZone:
                    description: |-
                      mysql parameter: Server Timezone. Default: UTC.
                      Server Timezone.
                    type: string
                  mysqlLogBin:
                    description: |-
                      mysql parameter: Binary Logging. Default: false.
                      Binary Logging.
                    type: boolean
                  mysqlMaxAllowedPacket:
                    description: |-
                      mysql parameter: Max Allowed Packet Size. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)). Default: 64M.
                      Max Allowed Packet Size. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)).
                    type: string
                  mysqlMaxConnections:
                    description: |-
                      mysql parameter: Max Connections. Default: 4000.
                      Max Connections.
                    type: number
                  mysqlQueryCache:
                    description: |-
                      mysql parameter: Enable query cache. Default: true.
                      Enable query cache.
                    type: boolean
                  mysqlQueryCacheLimit:
                    description: |-
                      mysql parameter: Query Cache Limit. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)). Default: 1M.
                      Query Cache Limit. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)).
                    type: string
                  mysqlQueryCacheSize:
                    description: |-
                      mysql parameter: Query Cache Size. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)). Default: 128M.
                      Query Cache Size. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)).
                    type: string
                  mysqlServerId:
                    description: |-
                      mysql parameter: Server Id. Default: 1.
                      Server Id.
                    type: number
                  mysqlSqlMode:
                    description: |-
                      mysql parameter: SQL Mode. Default: "ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_AUTO_CREATE_USER,NO_ENGINE_SUBSTITUTION".
                      SQL Mode.
                    type: string
                  name:
                    description: |-
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                    type: string
                  networkUuid:
                    description: |-
                      The UUID of the network that the service is attached to.
                      The UUID of the network that the service is attached to.
                    type: string
                  networkUuidRef:
                    description: Reference to a Network in gridscale to populate networkUuid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  networkUuidSelector:
                    description: Selector for a Network in gridscale to populate networkUuid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  performanceClass:
                    description: |-
                      Performance class of mysql service. Available performance classes at the time of writing: standard, high, insane, ultra.
                      Performance class of MySQL service.
                    type: string
                  release:
                    description: |-
                      The mysql release of this instance. For convenience, please use gscloud to get the list of available mysql service releases.
                      The MySQL release of this instance.\n
                      For convenience, please use gscloud https://github.com/gridscale/gscloud to get the list of available MySQL service releases.
                    type: string
                  securityZoneUuid:
                    description: |-
                      DEPRECATED  The UUID of the security zone that the service is attached to.
                      Security zone UUID linked to MySQL service.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  labels:
                    description: |-
                      List of labels in the format [ "label1", "label2" ].
                      List of labels.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  maxCoreCount:
                    description: |-
                      Maximum CPU core count. The mysql instance's CPU core count will be autoscaled based on the workload. The number of cores stays between 1 and max_core_count.
                      Maximum CPU core count. The MySQL instance's CPU core count will be autoscaled based on the workload. The number of cores stays between 1 and `max_core_count`.
                    type: number
                  mysqlBinlogFormat:
                    description: |-
                      mysql parameter: Binary Logging Format. Default: "ROW".
                      Binary Logging Format.
                    type: string
                  mysqlDefaultTimeZone:
                    description: |-
                      mysql parameter: Server Timezone. Default: UTC.
                      Server Timezone.
                    type: string
                  mysqlLogBin:
                    description: |-
                      mysql parameter: Binary Logging. Default: false.
                      Binary Logging.
                    type: boolean
                  mysqlMaxAllowedPacket:
                    description: |-
                      mysql parameter: Max Allowed Packet Size. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)). Default: 64M.
                      Max Allowed Packet Size. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)).
                    type: string
                  mysqlMaxConnections:
                    description: |-
                      mysql parameter: Max Connections. Default: 4000.
                      Max Connections.
                    type: number
                  mysqlQueryCache:
                    description: |-
                      mysql parameter: Enable query cache. Default: true.
                      Enable query cache.
                    type: boolean
                  mysqlQueryCacheLimit:
                    description: |-
                      mysql parameter: Query Cache Limit. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)). Default: 1M.
                      Query Cache Limit. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)).
                    type: string
                  mysqlQueryCacheSize:
                    description: |-
                      mysql parameter: Query Cache Size. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)). Default: 128M.
                      Query Cache Size. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)).
                    type: string
                  mysqlServerId:
                    description: |-
                      mysql parameter: Server Id. Default: 1.
                      Server Id.
                    type: number
                  mysqlSqlMode:
                    description: |-
                      mysql parameter: SQL Mode. Default: "ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_AUTO_CREATE_USER,NO_ENGINE_SUBSTITUTION".
                      SQL Mode.
                    type: string
                  name:
                    description: |-
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                    type: string
                  networkUuid:
                    description: |-
                      The UUID of the network that the service is attached to.
                      The UUID of the network that the service is attached to.
                    type: string
                  networkUuidRef:
                    description: Reference to a Network in gridscale to populate networkUuid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  networkUuidSelector:
                    description: Selector for a Network in gridscale to populate networkUuid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  performanceClass:
                    description: |-
                      Performance class of mysql service. Available performance classes at the time of writing: standard, high, insane, ultra.
                      Performance class of MySQL service.
                    type: string
                  release:
                    description: |-
                      The mysql release of this instance. For convenience, please use gscloud to get the list of available mysql service releases.
                      The MySQL release of this instance.\n
                      For convenience, please use gscloud https://github.com/gridscale/gscloud to get the list of available MySQL service releases.
                    type: string
                  securityZoneUuid:
                    description: |-
                      DEPRECATED  The UUID of the security zone that the service is attached to.
                      Security zone UUID linked to MySQL service.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.performanceClass is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.performanceClass)
                || (has(self.initProvider) && has(self.initProvider.performanceClass))'
            - message: spec.forProvider.release is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.release)
                || (has(self.initProvider) && has(self.initProvider.release))'
          status:
            description: MySQLStatus defines the observed state of MySQL.
            properties:
              atProvider:
                properties:
                  changeTime:
                    description: |-
                      Time of the last change.
                      Time of the last change.
                    type: string
                  createTime:
                    description: |-
                      Date time this service has been created.
                      Date time this service has been created.
                    type: string
                  id:
                    type: string
                  labels:
                    description: |-
                      List of labels in the format [ "label1", "label2" ].
                      List of labels.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  listenPort:
                    description: |-
                      The port numbers where this mysql service accepts connections.
                      The port numbers where this MySQL service accepts connections.
                    items:
                      properties:
                        host:
                          description: Host address.
                          type: string
                        name:
                          description: The human-readable name of the object. It supports
                            the full UTF-8 character set, with a maximum of 64 characters.
                          type: string
                        port:
                          type: number
                      type: object
                    type: array
                  maxCoreCount:
                    description: |-
                      Maximum CPU core count. The mysql instance's CPU core count will be autoscaled based on the workload. The number of cores stays between 1 and max_core_count.
                      Maximum CPU core count. The MySQL instance's CPU core count will be autoscaled based on the workload. The number of cores stays between 1 and `max_core_count`.
                    type: number
                  mysqlBinlogFormat:
                    description: |-
                      mysql parameter: Binary Logging Format. Default: "ROW".
                      Binary Logging Format.
                    type: string
                  mysqlDefaultTimeZone:
                    description: |-
                      mysql parameter: Server Timezone. Default: UTC.
                      Server Timezone.
                    type: string
                  mysqlLogBin:
                    description: |-
                      mysql parameter: Binary Logging. Default: false.
                      Binary Logging.
                    type: boolean
                  mysqlMaxAllowedPacket:
                    description: |-
                      mysql parameter: Max Allowed Packet Size. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)). Default: 64M.
                      Max Allowed Packet Size. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)).
                    type: string
                  mysqlMaxConnections:
                    description: |-
                      mysql parameter: Max Connections. Default: 4000.
                      Max Connections.
                    type: number
                  mysqlQueryCache:
                    description: |-
                      mysql parameter: Enable query cache. Default: true.
                      Enable query cache.
                    type: boolean
                  mysqlQueryCacheLimit:
                    description: |-
                      mysql parameter: Query Cache Limit. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)). Default: 1M.
                      Query Cache Limit. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)).
                    type: string
                  mysqlQueryCacheSize:
                    description: |-
                      mysql parameter: Query Cache Size. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)). Default: 128M.
                      Query Cache Size. Format: xM (where x is an integer, M stands for unit: k(kB), M(MB), G(GB)).
                    type: string
                  mysqlServerId:
                    description: |-
                      mysql parameter: Server Id. Default: 1.
                      Server Id.
                    type: number
                  mysqlSqlMode:
                    description: |-
                      mysql parameter: SQL Mode. Default: "ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_AUTO_CREATE_USER,NO_ENGINE_SUBSTITUTION".
                      SQL Mode.
                    type: string
                  name:
                    description: |-
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                    type: string
                  networkUuid:
                    description: |-
                      The UUID of the network that the service is attached to.
                      The UUID of the network that the service is attached to.
                    type: string
                  performanceClass:
                    description: |-
                      Performance class of mysql service. Available performance classes at the time of writing: standard, high, insane, ultra.
                      Performance class of MySQL service.
                    type: string
                  release:
                    description: |-
                      The mysql release of this instance. For convenience, please use gscloud to get the list of available mysql service releases.
                      The MySQL release of this instance.\n
                      For convenience, please use gscloud https://github.com/gridscale/gscloud to get the list of available MySQL service releases.
                    type: string
                  securityZoneUuid:
                    description: |-
                      DEPRECATED  The UUID of the security zone that the service is attached to.
                      Security zone UUID linked to MySQL service.
                    type: string
                  serviceTemplateCategory:
                    description: |-
                      The template service's category used to create the service.
                      The template service's category used to create the service.
                    type: string
                  serviceTemplateUuid:
                    description: |-
                      PaaS service template that mysql service uses.
                      PaaS service template that MySQL service uses.
                    type: string
                  status:
                    description: |-
                      Current status of PaaS service.
                      Current status of MySQL service.
                    type: string
                  usageInMinutes:
                    description: |-
                      Number of minutes that PaaS service is in use.
                      Number of minutes that MySQL service is in use.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
